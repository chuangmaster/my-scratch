{% paginate collections by 1 %}
<h1>list Collections</h1>

{% for collection in collections %}
<div>
  <a href="{{ collection.url }}">
    <h2>{{ collection.title }}</h2>
    <p>
      Products 數量: {{ collection.all_products_count }}
    </p>
  </a>
</div>
{% endfor %}
{{ paginate | default_pagination }}

{% endpaginate %}